server:
  port: ${PORT:9024}
spring:
  application:
    name: client-service-light
#eureka:
#  instance:
#    instanceId: ${random.uuid}
---
spring:
  profiles:
    include: security, eureka, ssl, truststore

