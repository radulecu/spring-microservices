server:
  port: ${PORT:8082}
spring:
  jpa:
    generate-ddl: true
  application:
    name: bookmark-service
#eureka:
#  instance:
#    instanceId: ${random.uuid}
---
spring:
  profiles:
    include: security, eureka, ssl, truststore