server:
  port: ${PORT:8082}
spring:
  jpa:
    generate-ddl: true
  application:
    name: bookmark-service
security:
  basic:
    enabled: false
#eureka:
#  instance:
#    instanceId: ${random.uuid}
---
spring:
  profiles:
    include: security, eureka, ssl, sslTrustStore, swagger