server:
  port: ${PORT:8083}
spring:
  jpa:
    generate-ddl: true
  application:
    name: contact-service
security:
  basic:
    enabled: false
#eureka:
#  instance:
#    instanceId: ${random.uuid}
---
spring:
  profiles:
    include: security, eureka, ssl, sslTrustStore

